<%= form_with(model: test_type) do |form| %>
  <% if test_type.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(test_type.errors.count, "error") %> prohibited this test_type from being saved:</h2>

      <ul>
        <% test_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :status, style: "display: block" %>
    <%= form.number_field :status %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
